<%- include ../layouts/header.ejs %>
<p>Zalogowano: <span><%= username %></span></p>

<button class="nav-button" onclick="addUser()">+ Dodaj użytkownika</button> 

<div class="page-data">
    <div class="data-table">
        <table>
        <tr>
            <th>Nr</th>
            <th>Nazwa użytkownika</th>
            <th>E-mail</th>
            <th>Uprawnienia isAdmin</th>
            <% if(isAdmin) { %> 
            <th>Akcja</th> 
            <% } %>
        </tr>
        <% if(data.length) {
            for(var i = 0; i< data.length; i++) {
                %>
                <tr>
                    <td><%= data[i].id %></td>
                    <td><%= data[i].username %></td>
                   
                    <td><%= data[i].email %></td>
                    <td><%= data[i].isAdmin %></td>
                   
                    <% if(isAdmin) { %> 
                        <td>
                            <a href="../users/edit/<%= data[i].id %>">Edycja</a>
                            <a href="../users/delete/<%= data[i].id %>">Usuń</a>
                        </td>
                        <% } %>
                </tr>
               
                <%
            }
        } else {
            %>
            <tr>
                <td colspan="6">Brak klientów</td>
            </tr>
            <%
        }
        %>
        </table>

    </div>
</div>
<%- include ../layouts/footer.ejs %>